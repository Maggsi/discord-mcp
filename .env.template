# MCP Server Configuration
HOST=127.0.0.1 # use 0.0.0.0 when running in Docker so the Container listens on all its ports
PORT=8000
LOG_LEVEL=INFO

# Discord Configuration
MAX_SHARDS=1
SESSION_TIMEOUT=300
RECONNECT_ATTEMPTS=5
RECONNECT_DELAY=1

# Event Streaming
BUFFER_SIZE=100
TIMEOUT=30
